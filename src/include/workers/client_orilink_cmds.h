#ifndef WORKERS_CLIENT_ORILINK_CMDS_H
#define WORKERS_CLIENT_ORILINK_CMDS_H

status_t hello1(const char *label, cow_c_session_t *session);
status_t hello2(const char *label, cow_c_session_t *session);

#endif
